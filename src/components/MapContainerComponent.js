import React, { useState } from 'react';

const MapContainerComponent = () => {
    const [state, setState] = useState(null); // Initialize the state

    return (
        <div>
            <h1>Map Container</h1>
        </div>
    );
};

export default MapContainerComponent;
